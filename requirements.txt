# Reconocimiento de voz
openai-whisper
sounddevice
numpy

# Traducción y OCR con IA
transformers
torch
sentencepiece  # requerido por los modelos Helsinki-NLP (Marian) para tokenizar
sacremoses     # requerido por algunos modelos Marian para pre/post-procesar texto

# Audio del sistema (loopback)
# soundcard funciona en Windows/Linux/macOS; en macOS requiere ademas
# un dispositivo virtual de loopback instalado aparte (ej. BlackHole).
soundcard

# Captura de pantalla y OCR clásico
mss
pillow
pytesseract

# Interfaz gráfica
customtkinter

# API
fastapi
uvicorn[standard]
python-multipart  # requerido por FastAPI para subir archivos (audio en /audio/transcribir)

# Empaquetado (solo para generar el .exe de Windows, no en produccion)
pyinstaller
